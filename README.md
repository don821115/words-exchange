# words-exchange
一个简单的词语映射转换工具，可依据自定义的规则转换文档里的特定词语，纯html单文件，跨系统可用。解决文档脱敏问题。

A simple word mapping and conversion tool that can transform specific words in documents based on custom rules. It is a pure HTML single file, cross-platform compatible, designed to solve document anonymization issues.
